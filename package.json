{
  "name": "testmedic-action",
  "private": true,
  "version": "0.1.0",
  "description": "Self-healing E2E tests: classifies Playwright failures, heals UI drift, reports real bugs",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test \"dist/test/*.test.js\""
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.211",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "^7.0.2"
  }
}
